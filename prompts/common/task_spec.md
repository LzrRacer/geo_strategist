# Task Specification Placeholder

Use this shared task specification for all experiment conditions after the
contracts are finalized.

Current scaffold requirements:

- Use only real-world inputs or explicitly approved scenario assumptions.
- Preserve provenance for every numeric claim.
- Do not invent, backfill, or simulate numeric datasets.
- Do not create fake API responses.
- Produce outputs that can be validated by deterministic scripts.

Business logic is intentionally out of scope in the scaffold phase.
